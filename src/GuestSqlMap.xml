<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
            PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
            "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<!-- SqlMap定義 -->
<sqlMap namespace="Guest">
	<select id="searchGuest" parameterClass="cn.spring.guest.form.GuestForm" resultClass="cn.spring.guest.form.GuestForm">
		SELECT   role,phone,email,id,name,password
		FROM guest_tab
		WHERE name=#name#
		AND password=#password#
	</select>

	<insert id="insertGuest" parameterClass="cn.spring.guest.form.GuestForm">
		INSERT  INTO   guest_tab(name, phone, email, password, role)
		 VALUES (#name#,#phone#, #email#, #password#, #role#)
	</insert>

</sqlMap>